/* Shared Tailwind font config — update font names here to match any new fonts added in fonts.css */
tailwind.config = {
  theme: {
    extend: {
      fontFamily: {
        display: ['Cinzel', 'serif'],
        decorative: ['"Cinzel Decorative"', 'serif'],
        body: ['"Crimson Text"', 'Georgia', 'serif'],
      }
    }
  }
}
